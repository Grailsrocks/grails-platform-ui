h1. ui:primaryNavigation

h2. Purpose

Renders the site's primary navigation menu.

Where the data for the navigation menu comes from is not defined by this tag. The default UI Set template uses the Platform Core Navigation API, but other UI Sets can use any API they like.

h2. Example

{code:xml}
<ui:primaryNavigation/>
{code}

h2. Attributes

None, they are all passed through to the UI template.



