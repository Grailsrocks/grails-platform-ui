h1. ui:primaryNavigation

h2. Purpose

Renders the site's primary navigation menu.

The default UI Set template uses the Platform Core Navigation API, but other UI Sets can use any API they like, although using the Platform Core API should provide the best interoperability with plugins.

h2. Example

{docx:xml}
<ui:primaryNavigation/>
{docx}

h2. Attributes

None, they are all passed through to the UI template.



